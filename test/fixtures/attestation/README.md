# Authentic attestation fixture

The wheel and bundle are copied unchanged from the GitHub CLI v2.100.0 test data:
https://github.com/cli/cli/tree/v2.100.0/pkg/cmd/attestation/test/data

Producer: actions/attest-demo, build-python.yml, source
a6c23b9806c593664f68637c8f9d45dfcf98b2db, run 8788389601, attempt 1.
The wheel is treated only as bytes; tests never install or execute it.
The fixture proves real signature verification, edited-byte rejection and signer
rejection. It is not evidence that this lab has been built or deployed.
